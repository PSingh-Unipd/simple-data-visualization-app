import { ChartMultiDataTransformingPipe } from './chart-multi-data-transforming.pipe';

describe('ChartMultiDataTransformingPipe', () => {
  it('create an instance', () => {
    const pipe = new ChartMultiDataTransformingPipe();
    expect(pipe).toBeTruthy();
  });
});
